pytest>=8.0
# Rodando como plugin, o Streamer Sidekick fornece o certifi (ele o embarca
# desde a v0.7.1). Fora dele, o Python do macOS nao usa o Keychain e qualquer
# download morre em CERTIFICATE_VERIFY_FAILED -- por isso ele entra aqui, no
# ambiente de desenvolvimento, e nao no requirements.txt do plugin.
certifi>=2024.2.2
